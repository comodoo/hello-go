# The container definition we want to use for developing our app
box: 
  id: golang
  cmd: /bin/sh
# Defining the dev pipeline
dev:
  steps:
    - script:
        code: |
          go build 
        reload: true
